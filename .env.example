# Database
# URL de conexão do Supabase (PostgreSQL)
# Formato: postgresql://user:password@host:port/database?schema=public
# Você pode encontrar essa URL no painel do Supabase em Settings > Database > Connection string
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# NextAuth.js
# Gere uma chave secreta aleatória para autenticação
# Você pode gerar usando: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"

# URL da aplicação
# Em desenvolvimento: http://localhost:3000
# Em produção: https://seu-dominio.com
NEXTAUTH_URL="http://localhost:3000"